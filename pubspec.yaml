name: bluetooth_led_control
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bluetooth_serial:
    path: ./local_plugins/flutter_bluetooth_serial
  provider: ^6.0.5
  cupertino_icons: ^1.0.5
  permission_handler: ^11.0.1


flutter:
  uses-material-design: true